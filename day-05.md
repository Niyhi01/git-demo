# Day 05 – 15 Dec 2025

Completed all 6 Git mastery tasks:

1. Local repo mastery – 5 files, 5 commits
2. Branching workflow – feature/login & feature/signup, merged to main
3. Conflict resolution – resolved login.sh merge conflict
4. Undo everything – reset, revert, checkout
5. Remote workflow – pushed to GitHub, pulled remote changes
6. Advanced – stash, tag, log, status

Key commands used:
- git init, git add, git commit, git status
- git branch, git checkout, git merge
- git reset, git revert
- git push, git pull
- git stash, git tag, git log --oneline, git status -s

Screenshot of `git log --oneline --graph --all` included.

I completed Day 5. No excuses.
